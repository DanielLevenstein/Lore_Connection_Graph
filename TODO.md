# Commit Working Changes
- Between each section in a TODO list commit working changes to create a stable checkpoint. 
- Do not commit changes until testing for each section is complete.
- If no testing section is present, add one to the TODO and commit it with the feature commit. 

## Semantic Aware Model
- Install semantic informed model locally for character rewrites and knowledge graph summary sections.
- Ensure the model selected is able to extract people and place names from existing source data successfully.
- Select a second visual inspection model to validate the generated UI from each page against the requirements. 

### Testing
- Validate that the selected semantic model config uses a runnable local GGUF artifact.
- Validate that the selected visual inspection model config is available for local UI screenshot review.
- Validate that semantic extraction finds people and places from existing fixture lore.
 
## UI Changes
- Split the existing character section into three tabs. [Characters, Places, Session Notes]
- Include the undo changes button for all entity types and write end-to-end tests validating that the button works when at least two changes are made to the entity. 

### Testing
- Write end-to-end test cases for the following.
  - Create a new character from the New Character UI. Load the created character in the UI and validate all fields.
  - Create a new place from the New Place UI. Load the saved place and validate all fields.
  - Create a new memory from the session notes section. Load the saved session note and validate info. 
- Repeat the test with characters created from the combined knowledge graph. 
- When end-to-end tests for all entity types pass commit changes. 

### Completed
- Added a Discord session-note importer and parsed `docs/lore/session_notes/DISCORD.md` into `YYYY-MM-DD_Session_Title.md` session note documents.
- Added `docs/SESSION_NOTES_TEMPLATE.md` for dated session note files.
- Added Delete Character, Delete Place, and Delete Session Note actions with file cleanup and UI status messages.
- Fixed create validation so failed character, place, and session note saves preserve entered fields.
- Added unit and end-to-end coverage for delete actions and failed-create draft preservation.

## Session Notes Attribute Graph

## Session Notes Attribute Graph Design 
- Write session notes attribute design doc before making code changes.

### Specs
- Add entity-specific Attribute graph sections for Session Notes and Places. 
- For extracted attributes ensure that each attribute has an attribute type field.
  - Character and Place attributes should have consistent formatting throughout the whole app.
  - Create a normalized format for names and places
    - If part of the name is unknown, create an id based on the known info like mr_smith, or john_adams_father
  - Suggested fields:
    - id:
    - edge_type: (currently called table)
    - edge_item:
    - edge_value:
    - edge_description:
    - edge_source:
  - Create a maximum length for the attribute description, and if it has to be shortened, make sure shortening is context-aware. 
### Testing
  - Validate attribute value for all characters and place attributes.
    - Don't include and _ characters in name attributes
    - Split the attribute value and description into two fields, ensuring each is of reasonable length. 
  - Scan for duplicate attribute generation.
### Committing Changes
- Run all tests before committing code.

## Character Rewrites
- Create a design document for implementing character rewrites. What model should we use?
  - Ensure that both the existing prose and the derived fields from the knowledge graph are used as source context for rewrites. 
- When a character has Autogenerated in the title, hide it from the character name in the UI.

### Testing
- Write a semantic aware end-to-end test which compares the original description to the auto-generated one and rates which one is better. 
  - Repeat the test until an auto-generated summary section for Orin_Nighbloom rates as better than the original backstory.
    - This is a very low bar as this character is basically AI slop to begin with. 


## Combined knowledge graph
Separate the knowledge store of the entity-specific knowledge base from the global knowledge base. 
- Graph nodes for the global knowledge base should only store Character or Place nodes. 
- Graph nodes for the character-specific knowledge should remain unchanged.

### Specifications
- Forbid self-referencing graph nodes.
- Don't show any nodes that don't have any connections to other nodes.
- Remove duplicate edges from the chart
- Only show true people and places in the graph.
- Ensure that unrelated mother and father characters are not combined in the graph.
- Add table to show detailed that are removed from the graph chart. 

### Testing
- Validate that each entity in the graph is either a Person or Place.
- Validate that no duplicate edges are shown in the graph.
- Visually scan the generated graph for entities that don't fit into the above specifications. 

## Character Rewrites
- Create a design document for implementing character rewrites. What model should we use?
  - Ensure that both the existing prose and the derived fields from the knowledge graph are used as source context for rewrites. 
- When a character has Autogenerated in the title, hide it from the character name in the UI
### Testing
- Write a semantic aware end-to-end test which compares the original description to the auto-generated one and rates which one is better. 
  - Repeat the test until an auto-generated summary section for Orin_Nighbloom rates as better than the original backstory.
    - This is a very low bar as this character is basically AI slop to begin with. 
