# Commit Working Changes
- Between each section in a TODO list commit working changes to create a stable checkpoint. 

## Character Rewrites

- Create a design document for implementing character rewrites. What model should we use?
  - Ensure that both the existing prose and the derived fields from the knowledge graph are used as source context for rewrites. 
- When a character has Autogenerated in the title, hide it from the character name in the UI 

### Specs
  - Support generation of backstory and summary text from the knowledge graph but only when the user specifically requests it.
    - Hide behind environmental variable by default. 
    - For auto-generated sections write Auto Generated next to the section title.
    - When saving a file, include both original and auto-generated sections.
    - Include the Undo button in the UI. 
    - UI buttons ["Populate Summary" , "Repopulate Summary" , "Rewrite Backstory", "Undo Changes" ]
- Implement a recursive undo changes button enabled whether this environmental variable is enabled

## Session Notes
- Write end-to-end test for session notes. User full UI test for this. 

### Specs
- Add a memory section in the app but don't tie it to any specific character or place.
- Session notes should be a plain text or Markdown file. If dates are present, create one session_notes file per calendar data. If not, use the current date.
- Save file under docs/lore/session_notes
- Add extensive unit tests for this feature. 

## Combined knowledge graph
- Forbid self-referencing graph nodes.
- Don't show any nodes that don't have any connections to other nodes.

### Specs
- Hide the combined knowledge graph in the UI behind an environmental variable.
  - parse all files in the doc/lore folder when generating the knowledge graph. 
  - Show all places and relationships in a combined graph even if they do not have full character sheets associated with them.
    - Show a one-word connection type between characters on the graph.
    - Show the table below with more detailed info.  
  - Update the knowledge graph UI so that each tab shows the graph for a different character, avoiding the need to manually load character files for the graph to populate.
  - Support the ability to create new place and character files from secondary characters. 
  - Add the ability to add the Character Connections section to the bottom of an existing character sheet. 
    - Generated Character Connections section should place character information sourced from other character sheets and places first.
    - If an existing character connection section exists, the two tables should be merged gracefully without knowledge loss.
  - When new places or characters are created through the knowledge graph, the existing character editor UI should show up and changes shouldn't be saved until the user clicks save. 

